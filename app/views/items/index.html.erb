<p id="notice"><%= notice %></p>

<h1>Items</h1>

<table>
  <thead>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Hotel</th>
    <th>Average Rating</th>
    <th>Comments Count</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody id="table-body">
  <% @items.each do |item| %>
    <%= render partial: 'items/item', locals: { item: item } %>
  <% end %>
  </tbody>
</table>
<p>
  <%== pagy_nav(@pagy) %>
</p>

<br>
<% if params[:hotel_id] %>
  <%= link_to 'New Item', new_hotel_item_path ,remote: true %> |
<% end %>

<%= link_to 'Back to hotel', hotels_path %>

<div id="new_form">

</div>

